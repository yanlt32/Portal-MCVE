{
  "name": "aviva-app",
  "version": "1.0.0",
  "description": "AVIVA - Portal dos Membros",
  "main": "server.js",
  "scripts": {
    "start": "pm2 start server.js --name aviva-server",
    "stop": "pm2 stop aviva-server",
    "restart": "pm2 restart aviva-server",
    "monitor": "pm2 monit",
    "logs": "pm2 logs aviva-server",
    "deploy": "pm2 deploy ecosystem.config.js production"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "pm2": "^5.3.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}